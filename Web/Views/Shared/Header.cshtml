@*<div class="navbar-collapse collapse">
    @{
        Html.RenderPartial("~/Views/Shared/Menu.cshtml");
        Html.RenderPartial("~/Views/Shared/Login.cshtml");
    }
</div>*@

@{ 
    //string actionName = ViewContext.Controller.ControllerContext.RouteData.Values["action"].ToString();
    string controllerName = ViewContext.Controller.ControllerContext.RouteData.Values["controller"].ToString();
}

<header id="header">
    <div class="top-bar">
        <div class="container">
            <div class="row">
                <div class="col-sm-6 col-xs-4">   
                </div>
                <div class="col-sm-6 col-xs-8">
                    @*<div class="social">
                        <div class="search">
                            <form role="form">
                                <input type="text" class="search-form" autocomplete="off" placeholder="O que você procura?">
                                <i class="fa fa-search"></i>
                            </form>
                        </div>
                    </div>*@
                    @{ 
                        Html.RenderPartial("~/Views/Shared/Login.cshtml");
                    }
                </div>
            </div>
        </div><!--/.container-->
    </div><!--/.top-bar-->
    <nav class="navbar navbar-inverse" role="banner">
        <div class="container">
            <div class="navbar-header">
                <img class="navbar-brand" src="http://www.feevale.br/cdn/topo-form/img/logo_feevale.png" alt="Universidade Feevale">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            </div>

            <div class="collapse navbar-collapse navbar-right">
                <ul class="nav navbar-nav">
                    
                    <li class="@(controllerName == "Home" ? "active" : "")">
                        @Html.ActionLink("Home", "Index", "Home")
                    </li>
                    <li class="@(controllerName == "Estabelecimento" ? "active" : "")">
                        @Html.ActionLink("Estabelecimentos", "Index", "Estabelecimento")
                    </li>
                    <li class="@(controllerName == "Produto" ? "active" : "")">
                        @Html.ActionLink("Produtos", "Index", "Produto")
                    </li>
                    <li class="@(controllerName == "Servico" ? "active" : "")">
                        @Html.ActionLink("Servicos", "Index", "Servico")
                    </li>
                    <li class="@(controllerName == "Contato" ? "active" : "")">
                        @Html.ActionLink("Contato", "Index", "Contato")
                    </li>

                </ul>
            </div>
        </div><!--/.container-->
    </nav><!--/nav-->

</header><!--/header-->